---
- hosts: all
  tasks:
    - name: Install required packages
      become: true
      apt:
        name:
          - aptitude
          - python3-pip
        state: latest
        update_cache: true

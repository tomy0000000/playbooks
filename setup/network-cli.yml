---
- hosts: all
  tasks:
    - name: Install packages
      become: true
      ansible.builtin.apt:
        name:
          - dnsutils # dig
          - iputils-ping # ping
          - whois # whois
          - dnsutils # nslookup
        state: latest
        update_cache: true
